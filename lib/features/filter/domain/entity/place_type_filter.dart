/// Class [PlaceTypeFilter] holds assets path and name needed for PlaceType in Filter feature
class PlaceTypeFilter {
  /// asset path
  String iconPath;

  /// title
  String title;

  /// Constructor
  PlaceTypeFilter({
    required this.iconPath,
    required this.title,
  });
}
